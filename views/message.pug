.messages 
  each type in Object.keys(messages)
    each message in messages[type]
      div(class="alert alert-"+type) #{message}
        //-receive reference from https://github.com/visionmedia/express-messages Using a custom template-Jade
